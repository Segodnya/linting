# Strict by default: each package only sees its declared deps, so phantom
# dependencies surface here instead of in a consumer's install. Do NOT enable
# shamefully-hoist — add a targeted public-hoist-pattern[] only for a proven gap.
link-workspace-packages=true
